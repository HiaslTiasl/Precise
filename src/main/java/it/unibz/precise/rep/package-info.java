/**
 * Spring Data repositories.
 * 
 * A repository acts as a DAO.
 * Each repository is exposed as a REST resource by Spring Data REST,
 * unless explicitly disabled using {@link RepositoryRestResource#exported()}.
 */
package it.unibz.precise.rep;